import styled from "styled-components";
import { Layout } from "antd";

export default styled(Layout)`
  .header .logo {
    color: #fff;
  }
  .content {
    padding: 0 50px;
    height: 100%;
    .layout {
      height: 100%;
    }
  }
`;
